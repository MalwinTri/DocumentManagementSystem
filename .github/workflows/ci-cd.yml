name: CI/CD

on:
  pull_request:
    branches: ["master", "CI_CD_Pipeline"]
  push:
    branches: ["master", "CI_CD_Pipeline"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  DOTNET_VERSION: "8.0.x"
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/dms

jobs:
  build-and-test:
    name: Build, Lint & Test
    runs-on: ubuntu-latest

    # Wichtig: AWS-Creds job-weit setzen, damit aws s3 ls im "Wait for Garage" klappt
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.CI_S3_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.CI_S3_SECRET_KEY }}
      AWS_DEFAULT_REGION: ${{ secrets.CI_S3_REGION }}

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: ${{ secrets.CI_DB_USER }}
          POSTGRES_PASSWORD: ${{ secrets.CI_DB_PASSWORD }}
          POSTGRES_DB: ${{ secrets.CI_DB_NAME }}
        ports:
          - 5433:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d dms"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=10

      garage:
        image: dxflrs/garage:v2.1.0
        ports:
          - 3900:3900

      rabbitmq:
        image: rabbitmq:3-management
        ports:
          - 5672:5672
          - 15672:15672

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-restore

      # -----------------------
      # LINTING (dotnet format)
      # -----------------------
      - name: Install dotnet format
        run: dotnet tool install -g dotnet-format

      - name: Add dotnet tools to PATH
        run: echo "$HOME/.dotnet/tools" >> $GITHUB_PATH

      - name: Lint (dotnet format)
        run: dotnet format --verify-no-changes --verbosity minimal

      # -----------------------
      # Unit Tests (exclude OCR smoke)
      # -----------------------
      - name: Unit Tests (exclude OCR smoke)
        run: dotnet test ./DocumentManagementSystem.Tests/DocumentManagementSystem.Tests.csproj -c Release --no-build --verbosity normal --filter "Category!=OcrSmoke"

      # -----------------------
      # Wait for Postgres
      # -----------------------
      - name: Install postgres client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Wait for Postgres
        run: |
          for i in {1..30}; do
            pg_isready -h localhost -p 5433 -U "${{ secrets.CI_DB_USER }}" -d "${{ secrets.CI_DB_NAME }}" && exit 0
            echo "Postgres not ready yet... ($i/30)"
            sleep 2
          done
          echo "Postgres did not become ready in time"
          exit 1

      # -----------------------
      # Ensure AWS CLI is available
      # -----------------------
      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      # -----------------------
      # Debug Garage container (shows if it crashed)
      # -----------------------
      - name: Show Garage logs
        if: always()
        run: |
          docker ps -a
          docker logs "${{ job.services.garage.id }}" || true

      # -----------------------
      # Wait for Garage service (S3-Check)
      # -----------------------
      - name: Wait for Garage service
        run: |
          echo "Using endpoint: http://garage:3900"
          echo "AWS_DEFAULT_REGION=$AWS_DEFAULT_REGION"
          for i in {1..30}; do
            echo "Attempt $i/30..."
            if aws --endpoint-url http://garage:3900 s3 ls --no-verify-ssl >/dev/null 2>&1; then
              echo "Garage is ready!"
              exit 0
            fi
            echo "Garage not ready yet... ($i/30)"
            sleep 5
          done
          echo "Garage did not become ready in time"
          exit 1

      # -----------------------
      # Wait for RabbitMQ (Management UI)
      # -----------------------
      - name: Wait for RabbitMQ
        run: |
          for i in {1..30}; do
            curl --silent --fail http://rabbitmq:15672 >/dev/null && exit 0
            echo "RabbitMQ not ready yet... ($i/30)"
            sleep 2
          done
          echo "RabbitMQ did not become ready in time"
          exit 1

      # -----------------------
      # Integration Tests
      # -----------------------
      - name: Integration Tests
        env:
          ConnectionStrings__Default: Host=localhost;Port=5433;Database=${{ secrets.CI_DB_NAME }};Username=${{ secrets.CI_DB_USER }};Password=${{ secrets.CI_DB_PASSWORD }}
          GarageS3__Endpoint: http://garage:3900
          GarageS3__AccessKey: ${{ secrets.CI_S3_ACCESS_KEY }}
          GarageS3__SecretKey: ${{ secrets.CI_S3_SECRET_KEY }}
          GarageS3__Bucket: ${{ secrets.CI_S3_BUCKET }}
          GarageS3__Region: ${{ secrets.CI_S3_REGION }}
          RABBIT_HOST: rabbitmq
          RABBIT_USER: guest
          RABBIT_PASS: guest
          RABBIT_QUEUE: ocr-queue
        run: dotnet test ./DocumentManagementSystem.IntegrationTests/DocumentManagementSystem.IntegrationTests.csproj -c Release --no-build --verbosity normal

  docker-build-push:
    name: Docker Build & Push (master / CI_CD_Pipeline)
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/CI_CD_Pipeline'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & Push API
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./DocumentManagementSystem/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api:${{ github.sha }}

      - name: Build & Push OCR Worker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./OCR_Worker/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-ocr:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-ocr:${{ github.sha }}

      - name: Build & Push GenAI Worker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./DocumentManagementSystem.GenAI_Worker/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-genai:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-genai:${{ github.sha }}

      - name: Build & Push AccessBatch
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./DocumentManagementSystem.AccessBatch/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-accessbatch:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-accessbatch:${{ github.sha }}
